[package]
name = "mi-plaits"
description = "Rust port of Mutable Instruments Plaits (Macro oscillator (24 synthesis + noise models)). SCAFFOLD -- DSP not yet ported."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lib]
name = "plaits"
path = "src/lib.rs"

[dependencies]
mi-stmlib.workspace = true
