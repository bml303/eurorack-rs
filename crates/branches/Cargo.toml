[package]
name = "mi-branches"
description = "Rust port of Mutable Instruments Branches (Dual Bernoulli gate). SCAFFOLD -- DSP not yet ported."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lib]
name = "branches"
path = "src/lib.rs"

[dependencies]
mi-stmlib.workspace = true
