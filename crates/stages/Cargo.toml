[package]
name = "mi-stages"
description = "Rust port of Mutable Instruments Stages (Segment generator). SCAFFOLD -- DSP not yet ported."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lib]
name = "stages"
path = "src/lib.rs"

[dependencies]
mi-stmlib.workspace = true
