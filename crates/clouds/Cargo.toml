[package]
name = "mi-clouds"
description = "Rust port of Mutable Instruments Clouds (Granular texture synthesizer). SCAFFOLD -- DSP not yet ported."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lib]
name = "clouds"
path = "src/lib.rs"

[dependencies]
mi-stmlib.workspace = true
