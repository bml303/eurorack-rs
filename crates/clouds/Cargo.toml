[package]
name = "mi-clouds"
description = "Rust port of Mutable Instruments Clouds (granular texture synthesizer)."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lib]
name = "clouds"
path = "src/lib.rs"

[dependencies]
mi-stmlib.workspace = true
libm = "0.2"

[dev-dependencies]
libm = "0.2"
