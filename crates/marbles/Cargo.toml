[package]
name = "mi-marbles"
description = "Rust port of Mutable Instruments Marbles (Random sampler / CV generator). SCAFFOLD -- DSP not yet ported."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lib]
name = "marbles"
path = "src/lib.rs"

[dependencies]
mi-stmlib.workspace = true
