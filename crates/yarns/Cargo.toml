[package]
name = "mi-yarns"
description = "Rust port of Mutable Instruments Yarns (Monophonic / polyphonic MIDI interface). SCAFFOLD -- DSP not yet ported."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lib]
name = "yarns"
path = "src/lib.rs"

[dependencies]
mi-stmlib.workspace = true
