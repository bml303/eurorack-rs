[package]
name = "mi-yarns"
description = "Rust port of Mutable Instruments Yarns (Monophonic / polyphonic MIDI interface). Voice/oscillator/tuning/clock ported; part/multi/MIDI routing not yet."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lib]
name = "yarns"
path = "src/lib.rs"

[dependencies]
mi-stmlib.workspace = true
