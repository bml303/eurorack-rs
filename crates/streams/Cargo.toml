[package]
name = "mi-streams"
description = "Rust port of Mutable Instruments Streams (Dual dynamics gate (VCA/VCF + envelope follower)). SCAFFOLD -- DSP not yet ported."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lib]
name = "streams"
path = "src/lib.rs"

[dependencies]
mi-stmlib.workspace = true
